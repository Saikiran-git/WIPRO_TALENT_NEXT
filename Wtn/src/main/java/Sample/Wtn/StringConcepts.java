package Sample.Wtn;

public class StringConcepts {

}

/*
declaration of a string : char data	[] = {'a', 'b', 'c'};
String s = new String() -> is a default constructor for string.
Length of the string : String s = sc.next(); strlen(s);
For combination of strings : System.out.println("My name is"+s+".");
To print length of known string is : System.out.println("Kiran".length());
-Retireving the charecters in a string can be done as:
	public charAt(int index)
	char c;
  	c = "abc".charAt(1); //here we printing the charecter at position 1 here i.e "b";

*/
